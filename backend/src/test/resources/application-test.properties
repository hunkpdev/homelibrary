# Test-specific overrides — datasource comes from the active environment
# (local: application-local.properties via -P local, CI: PostgreSQL env vars)
app.jwt.secret=test-secret-key-for-integration-tests-min32chars!!
app.cookie.secure=false
